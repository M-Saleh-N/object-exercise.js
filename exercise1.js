const car = {
    brand : 'Volvo',
    model : 'EX90'
  };

  //There are two correct syntax to alert Volvo, name them

  //1. dotted notation
console.log(car.brand);

  //2. bracket notation
console.log(car['brand']);
